name: al_nomani_shared
description: Shared money, permissions, sync protocol, and identifiers for Al Nomani ERP.
publish_to: none
version: 1.0.0

environment:
  sdk: ^3.11.5

dependencies:
  meta: ^1.16.0
  timezone: ^0.10.0
  uuid: ^4.5.1

dev_dependencies:
  test: ^1.25.0
  lints: ^6.0.0
