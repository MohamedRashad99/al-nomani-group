-- DEMO / DEVELOPMENT SEED ONLY.
-- Must never run against a production database that already has business data.
-- Guard in application code: allow_seed=true AND tables empty.

-- Placeholder: actual seed is applied by server/lib/database/seeder.dart
-- and Flutter local SeedService so password hashes stay consistent.
SELECT 1;
