name: al_nomani_server
description: Al Nomani ERP Dart backend — PostgreSQL, JWT, Google Sheets backup.
publish_to: none
version: 1.0.0

environment:
  sdk: ^3.11.5

dependencies:
  al_nomani_shared:
    path: ../packages/shared
  shelf: ^1.4.2
  shelf_router: ^1.1.4
  shelf_cors_headers: ^0.1.5
  postgres: ^3.5.6
  dart_jsonwebtoken: ^3.2.0
  bcrypt: ^1.2.0
  googleapis: ^14.0.0
  googleapis_auth: ^2.0.0
  http: ^1.5.0
  uuid: ^4.5.1

dev_dependencies:
  lints: ^6.0.0
  test: ^1.31.2
